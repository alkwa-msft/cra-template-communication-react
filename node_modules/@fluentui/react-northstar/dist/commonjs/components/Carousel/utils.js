"use strict";

exports.__esModule = true;
exports.getAnimationName = getAnimationName;

function getAnimationName(_ref) {
  var active = _ref.active,
      dir = _ref.dir,
      animationEnterFromPrev = _ref.animationEnterFromPrev,
      animationEnterFromNext = _ref.animationEnterFromNext,
      animationExitToPrev = _ref.animationExitToPrev,
      animationExitToNext = _ref.animationExitToNext;
  var initialMounting = typeof dir === 'undefined';

  if (!initialMounting) {
    if (!active) {
      return dir === 'start' ? animationExitToPrev : animationExitToNext;
    }

    if (active) {
      return dir === 'start' ? animationEnterFromNext : animationEnterFromPrev;
    }
  }

  return '';
}
//# sourceMappingURL=utils.js.map
